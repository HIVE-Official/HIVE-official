// Bounded Context Owner: Identity & Access Management Guild
"use client";

export function Vignette(): JSX.Element {
  return (
    <div
      aria-hidden
      className="absolute inset-0 [mask-image:radial-gradient(ellipse_at_center,black_65%,transparent_100%)]"
      style={{ background: "radial-gradient(ellipse at center, rgba(0,0,0,0) 60%, rgba(0,0,0,0.25) 100%)" }}
    />
  );
}
