// Temporary stub for firebase client utilities
// TODO: Use proper firebase client from @hive/core

import { db as firebaseDb } from '@hive/core';

// Re-export the client db for compatibility
export const db = firebaseDb; 