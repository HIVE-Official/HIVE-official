{
  "buildCommand": "pnpm install && pnpm turbo run build --filter=web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install -g pnpm@9.1.1 && pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "functions": {
    "apps/web/src/app/api/**": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "s-maxage=0, stale-while-revalidate"
        }
      ]
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
