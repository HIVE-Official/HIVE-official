// @ts-check
// Self-contained logger for core package - no external dependencies
export const logger = {
    info: (message, ...args) => {
        if (process.env.NODE_ENV !== "production") {
            console.info(`[CORE-INFO] ${message}`, ...args);
        }
    },
    error: (message, error) => {
        // Always log errors
        console.error(`[CORE-ERROR] ${message}`, error);
    },
    warn: (message, ...args) => {
        if (process.env.NODE_ENV !== "production") {
            console.warn(`[CORE-WARN] ${message}`, ...args);
        }
    }
    debug: (message, ...args) => {
        if (process.env.NODE_ENV === "development") {
            console.debug(`[CORE-DEBUG] ${message}`, ...args);
        }
    }
};
//# sourceMappingURL=logger.js.map