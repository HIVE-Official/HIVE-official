// Re-export Firebase admin utilities for easier importing
export { dbAdmin, adminFirestore, firebaseAuth, isFirebaseConfigured } from '../firebase-admin';
export { app, auth, db } from '../firebase';