{
  "name": "@hive/firebase",
  "version": "1.0.0",
  "description": "Firebase configuration and initialization for HIVE",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "typecheck": "npx tsc --noEmit",
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch"
  },
  "dependencies": {
    "firebase": "^10.12.2",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "tsup": "^7.2.0",
    "typescript": "^5.3.3"
  }
}