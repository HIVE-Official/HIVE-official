/**
 * HIVE Modal Component;
 * Re-export from existing implementation with design system interface;
 */

export { HiveModal as Modal, type HiveModalProps as ModalProps } from '../../../components/hive-modal';

// Design system specific variants;
export { hiveModalVariants as modalVariants } from '../../../components/hive-modal';