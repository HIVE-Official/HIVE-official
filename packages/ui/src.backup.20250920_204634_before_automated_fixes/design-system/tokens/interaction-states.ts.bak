/**
 * HIVE Interaction State Specifications
 * Precise State Definitions for Buttons and Inputs
 */

// Button State Definitions - Comprehensive interaction mapping
export const buttonStates: Record<string, unknown> = {
  // Default State default: {
    border: '1px solid rgba(var(--hive-gold-rgb), 0.4)',
    background: 'transparent',
    text: 'var(--hive-gold)',
    transform: 'none',
    transition: 'all 200ms cubic-bezier(0.4, 0.0, 0.6, 1.0)',
    use: 'Resting state, no user interaction'
  }

  // Hover State (Desktop Only) hover: {
    border: '1px solid rgba(var(--hive-gold-rgb), 0.8)',
    background: 'rgba(var(--hive-gold-rgb), 0.05)',
    text: 'var(--hive-gold)',
    transform: 'translateY(-1px) scale(1.01)',
    transition: 'all 200ms cubic-bezier(0.4, 0.0, 0.6, 1.0)',
    use: 'Desktop pointer hover, not on touch devices',
    condition: '@media (hover: hover) and (pointer: fine)'
  }

  // Active State active: {
    border: '1px solid var(--hive-gold)',
    background: 'rgba(var(--hive-gold-rgb), 0.12)',
    text: 'var(--hive-gold)',
    transform: 'translateY(0px) scale(0.98)',
    transition: 'all 100ms cubic-bezier(0.25, 0.46, 0.45, 0.94)',
    use: 'Pressed/clicked state, immediate feedback'
  }

  // Focus State focus: {
    border: '1px solid var(--hive-gold)',
    background: 'rgba(var(--hive-gold-rgb), 0.05)',
    text: 'var(--hive-gold)',
    boxShadow: '0 0 0 3px rgba(var(--hive-gold-rgb), 0.3)',
    transform: 'none',
    outline: 'none',
    use: 'Keyboard focus, accessibility compliance'
  }

  // Disabled State disabled: {
    border: '1px solid rgba(255, 255, 255, 0.1)',
    background: 'transparent',
    text: 'rgba(255, 255, 255, 0.3)',
    transform: 'none',
    pointerEvents: 'none',
    cursor: 'not-allowed',
    use: 'Non-interactive state'
  }

  // Loading State loading: {
    border: '1px solid rgba(var(--hive-gold-rgb), 0.4)',
    background: 'rgba(var(--hive-gold-rgb), 0.05)',
    text: 'transparent',
    pointerEvents: 'none',
    cursor: 'wait',
    use: 'Processing state with spinner overlay'
  }
} as const;

// Form Input State Definitions - Complete interaction coverage
export const inputStates: Record<string, unknown> = {
  // Default State default: {
    border: '1px solid rgba(255, 255, 255, 0.2)',
    background: 'rgba(255, 255, 255, 0.02)',
    text: 'rgba(255, 255, 255, 0.9)',
    placeholder: 'rgba(255, 255, 255, 0.5)',
    outline: 'none',
    transition: 'all 200ms cubic-bezier(0.4, 0.0, 0.6, 1.0)',
    use: 'Default input appearance'
  }

  // Focus State focus: {
    border: '1px solid var(--hive-gold)',
    background: 'rgba(255, 255, 255, 0.03)',
    text: 'rgba(255, 255, 255, 1.0)',
    boxShadow: '0 0 0 3px rgba(var(--hive-gold-rgb), 0.2)',
    outline: 'none',
    use: 'Active input focus'
  }

  // Hover State (Desktop) hover: {
    border: '1px solid rgba(255, 255, 255, 0.3)',
    background: 'rgba(255, 255, 255, 0.025)',
    condition: '@media (hover: hover) and (pointer: fine)',
    use: 'Desktop hover before focus'
  }

  // Filled State filled: {
    border: '1px solid rgba(255, 255, 255, 0.25)',
    background: 'rgba(255, 255, 255, 0.03)',
    text: 'rgba(255, 255, 255, 1.0)',
    use: 'Input with valid content'
  }

  // Error State error: {
    border: '1px solid #F87171',
    background: 'rgba(248, 113, 113, 0.03)',
    text: 'rgba(255, 255, 255, 1.0)',
    boxShadow: '0 0 0 3px rgba(248, 113, 113, 0.2)',
    use: 'Validation error state'
  }

  // Success State success: {
    border: '1px solid #34D399',
    background: 'rgba(52, 211, 153, 0.03)',
    text: 'rgba(255, 255, 255, 1.0)',
    boxShadow: '0 0 0 3px rgba(52, 211, 153, 0.2)',
    use: 'Validation success state'
  }

  // Disabled State disabled: {
    border: '1px solid rgba(255, 255, 255, 0.1)',
    background: 'rgba(255, 255, 255, 0.01)',
    text: 'rgba(255, 255, 255, 0.3)',
    placeholder: 'rgba(255, 255, 255, 0.2)',
    pointerEvents: 'none',
    cursor: 'not-allowed',
    use: 'Non-editable input'
  }

  // Read-only State readonly: {
    border: '1px solid rgba(255, 255, 255, 0.15)',
    background: 'rgba(255, 255, 255, 0.02)',
    text: 'rgba(255, 255, 255, 0.8)',
    cursor: 'default',
    use: 'Display-only input'
  }
} as const;

// State Transition Rules - Animation choreography
export const stateTransitions: Record<string, unknown> = {
  // Duration by interaction type durations: {
    immediate: '100ms',  // Active/pressed feedback  standard: '200ms',   // Hover, focus changes validation: '300ms'  // Error/success states
  }

  // Easing by state change easing: {
    enter: 'cubic-bezier(0.4, 0.0, 0.2, 1.0)',    // Smooth entry  exit: 'cubic-bezier(0.4, 0.0, 1, 1)',         // Smooth exit  feedback: 'cubic-bezier(0.25, 0.46, 0.45, 0.94)', // Snappy feedback natural: 'cubic-bezier(0.4, 0.0, 0.6, 1.0)'   // Default transitions
  }

  // Properties that should animate
        animatableProperties: [
    'border-color',
    'background-color',
    'color',
    'box-shadow',
    'transform',
    'opacity'
  ],

  // Properties to avoid animating (performance)
        avoidAnimating: [
    'width',
    'height', 
    'padding',
    'margin',
    'top',
    'left',
    'right',
    'bottom'
  ]
} as const;

// Touch Device Optimizations - Mobile-first considerations
export const touchOptimizations: Record<string, unknown> = {
  // Touch target requirements minimumTouchTarget: '44px'  
  // Touch-specific states touchActive: {
    // No hover states on touch devices removeHover: true    
    // Enhanced active/pressed feedback enhancedFeedback: {
      transform: 'scale(0.96)',
      duration: '100ms',
      hapticFeedback: 'light' // For devices that support it
    }
  }

  // Touch interaction patterns patterns: {
    tap: 'Single quick press for primary actions',
    longPress: '500ms hold for secondary actions',
    doubleTab: 'Double tap within 300ms for special actions'
  }
} as const;

// Accessibility Requirements - WCAG compliance
export const accessibilityRequirements: Record<string, unknown> = {
  // Focus management focus: {
    visible: 'Focus must be clearly visible',
    contrast: 'Minimum 3:1 contrast ratio for focus indicators',
    persistent: 'Focus state must persist until next interaction'
  }

  // Keyboard navigation keyboard: {
    tabOrder: 'Logical tab sequence required',
    enterKey: 'Enter key activates buttons',
    spaceKey: 'Space key toggles checkboxes/switches',
    escapeKey: 'Escape closes modals and dropdowns'
  }

  // Screen reader support screenReader: {
    stateAnnouncements: 'State changes must be announced',
    errorMessages: 'Error descriptions must be associated with inputs',
    loadingStates: 'Loading states must indicate progress'
  }

  // Color accessibility colorCompliance: {
    colorBlindness: 'Information not conveyed by color alone',
    highContrast: 'Support for high contrast mode',
    darkMode: 'All states work in dark mode'
  }
} as const;

// Component-Specific State Variations
export const componentVariations: Record<string, unknown> = {
  // Primary vs Secondary buttons buttonVariants: {
    primary: {
      default: buttonStates.default,
      hover: buttonStates.hover,
      active: buttonStates.active,
      focus: buttonStates.focus
    }
    
    secondary: {
      default: {
        ...buttonStates.default,
        border: '1px solid rgba(255, 255, 255, 0.2)',
        text: 'rgba(255, 255, 255, 0.8)'
      }
      hover: {
        ...buttonStates.hover,
        border: '1px solid rgba(255, 255, 255, 0.4)',
        background: 'rgba(255, 255, 255, 0.05)'
      }
    }
    
    ghost: {
      default: {
        ...buttonStates.default,
        border: 'none',
        background: 'transparent'
      }
      hover: {
        ...buttonStates.hover,
        border: 'none',
        background: 'rgba(255, 255, 255, 0.05)'
      }
    }
  }

  // Input type variations inputVariants: {
    text: inputStates.default,
    email: inputStates.default,
    password: {
      ...inputStates.default,
      // Special considerations for password visibility toggle passwordToggle: {
        position: 'absolute',
        right: '12px',
        top: '50%',
        transform: 'translateY(-50%)'
      }
    }
    search: {
      ...inputStates.default,
      // Search-specific styling searchIcon: {
        position: 'absolute',
        left: '12px',
        top: '50%',
        transform: 'translateY(-50%)'
      }
    }
  }
} as const;

export const interactionStates: Record<string, unknown> = {
  buttonStates,
  inputStates,
  stateTransitions,
  touchOptimizations,
  accessibilityRequirements,
  componentVariations
} as const;