/**
 * HIVE Card Component
 * Re-export from existing implementation with design system interface
 */

export { Card, type CardProps } from '../../../atomic/ui/card';

// Design system specific variants
export { cardVariants } from '../../../atomic/ui/card';