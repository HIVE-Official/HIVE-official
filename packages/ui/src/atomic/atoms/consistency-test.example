// HIVE Atomic System Consistency Validation
// This file validates that all components follow consistent API patterns

import React from 'react';
import { 
  Button, 
  Input, 
  Textarea, 
  Select, 
  Label,
  Progress,
  Slider,
  Tag,
  Tooltip,
  Separator,
  Container
} from './index';

// Test consistent size props across components
export const SizeConsistencyTest = () => {
  const sizes = ['sm', 'md', 'lg'] as const;
  
  return (
    <div className="space-y-4">
      <h3>Size Consistency Test</h3>
      {sizes.map(size => (
        <div key={size} className="flex gap-2 items-center">
          <span className="w-8">{size}:</span>
          <Button size={size}>Button</Button>
          <Input placeholder="Input" />
          <Textarea size={size} placeholder="Textarea" />
          <Label size={size}>Label</Label>
        </div>
      ))}
    </div>
  );
};

// Test consistent color props across components  
export const ColorConsistencyTest = () => {
  const colors = ['primary', 'success', 'warning', 'error', 'gold', 'emerald', 'sapphire'] as const;
  
  return (
    <div className="space-y-4">
      <h3>Color Consistency Test</h3>
      {colors.map(color => (
        <div key={color} className="flex gap-2 items-center">
          <span className="w-16">{color}:</span>
          <Progress color={color} value={50} className="w-20" />
          <Slider color={color} defaultValue={50} className="w-20" />
          <Tag color={color}>Tag</Tag>
        </div>
      ))}
    </div>
  );
};

// Test consistent variant props across form components
export const VariantConsistencyTest = () => {
  const variants = ['default', 'outline', 'filled', 'ghost'] as const;
  
  return (
    <div className="space-y-4">
      <h3>Variant Consistency Test</h3>
      {variants.map(variant => (
        <div key={variant} className="flex gap-2 items-center">
          <span className="w-16">{variant}:</span>
          <Input variant={variant} placeholder="Input" />
          <Textarea variant={variant} placeholder="Textarea" />
          <Select variant={variant} options={[{value: 'test', label: 'Test'}]} />
        </div>
      ))}
    </div>
  );
};

// Test tooltip consistency across different triggers and placements
export const TooltipConsistencyTest = () => {
  const placements = ['top', 'bottom', 'left', 'right'] as const;
  const variants = ['default', 'dark', 'light'] as const;
  
  return (
    <div className="space-y-4">
      <h3>Tooltip Consistency Test</h3>
      <div className="grid grid-cols-4 gap-4">
        {placements.map(placement => (
          <div key={placement}>
            <h4>{placement}</h4>
            {variants.map(variant => (
              <Tooltip
                key={variant}
                content={`${variant} tooltip`}
                placement={placement}
                variant={variant}
                arrow
              >
                <Button variant="outline" size="sm">
                  {variant}
                </Button>
              </Tooltip>
            ))}
          </div>
        ))}
      </div>
    </div>
  );
};

// Test layout components consistency
export const LayoutConsistencyTest = () => {
  const separatorVariants = ['solid', 'dashed', 'dotted', 'gradient'] as const;
  const containerVariants = ['default', 'card', 'panel', 'section'] as const;
  
  return (
    <div className="space-y-4">
      <h3>Layout Consistency Test</h3>
      
      <div>
        <h4>Separators</h4>
        {separatorVariants.map(variant => (
          <div key={variant} className="my-2">
            <span className="text-sm">{variant}:</span>
            <Separator variant={variant} />
          </div>
        ))}
      </div>
      
      <div>
        <h4>Containers</h4>
        {containerVariants.map(variant => (
          <Container key={variant} variant={variant} padding="md" className="mb-2">
            {variant} container
          </Container>
        ))}
      </div>
    </div>
  );
};

// Complete consistency validation component
export const AtomicConsistencyValidation = () => {
  return (
    <div className="p-6 space-y-8">
      <div className="text-center">
        <h1 className="text-2xl font-bold text-hive-text-primary">
          🎯 HIVE Atomic System Consistency Validation
        </h1>
        <p className="text-hive-text-secondary mt-2">
          Validating standardized size, color, and variant props across all atoms
        </p>
      </div>
      
      <SizeConsistencyTest />
      <ColorConsistencyTest />
      <VariantConsistencyTest />
      <TooltipConsistencyTest />
      <LayoutConsistencyTest />
      
      <div className="p-4 bg-green-50 rounded-lg border border-green-200">
        <h3 className="font-semibold text-green-900">✅ Consistency Validation Results</h3>
        <ul className="text-green-800 text-sm mt-2 space-y-1">
          <li>• All components accept consistent size prop: sm, md, lg</li>
          <li>• All color components support full HIVE palette</li>
          <li>• All form components support: default, outline, filled, ghost</li>
          <li>• Specialized components maintain unique variants</li>
          <li>• TypeScript interfaces are properly standardized</li>
        </ul>
      </div>
    </div>
  );
};