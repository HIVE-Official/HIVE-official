import type { Meta, StoryObj } from "@storybook/react";

// Mock the feed page component for Storybook
const FeedPageComponent = ({
  userName = "Sarah",
  userEmail = "sarah.johnson@stanford.edu",
  hasContent = false,
  loadingState = "loaded",
}: {
  userName?: string;
  userEmail?: string;
  hasContent?: boolean;
  loadingState?: "loading" | "loaded";
}) => {
  if (loadingState === "loading") {
    return (
      <div className="min-h-screen bg-black flex items-center justify-center">
        <div className="w-8 h-8 border-2 border-yellow-500 border-t-transparent rounded-full animate-spin" />
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-black">
      {/* Header */}
      <header className="sticky top-0 z-50 bg-black/95 backdrop-blur-sm border-b border-zinc-800">
        <div className="max-w-6xl mx-auto px-4 h-16 flex items-center justify-between">
          <div className="flex items-center gap-6">
            <h1 className="text-xl font-bold text-white">HIVE</h1>
            <nav className="hidden md:flex items-center gap-6">
              <button className="flex items-center gap-2 text-white bg-yellow-500/10 px-3 py-1.5 rounded-md">
                🏠 Feed
              </button>
              <button className="flex items-center gap-2 text-zinc-400 hover:text-white px-3 py-1.5 rounded-md hover:bg-zinc-800">
                👥 Spaces
              </button>
              <button className="flex items-center gap-2 text-zinc-400 hover:text-white px-3 py-1.5 rounded-md hover:bg-zinc-800">
                📅 Events
              </button>
            </nav>
          </div>

          <div className="flex items-center gap-3">
            <button className="p-2 text-zinc-400 hover:text-white rounded-md hover:bg-zinc-800">
              🔍
            </button>
            <button className="p-2 text-zinc-400 hover:text-white rounded-md hover:bg-zinc-800">
              🔔
            </button>
            <button className="p-2 text-zinc-400 hover:text-white rounded-md hover:bg-zinc-800">
              👤
            </button>
            <button className="p-2 text-zinc-400 hover:text-white rounded-md hover:bg-zinc-800">
              ⚙️
            </button>
          </div>
        </div>
      </header>

      {/* Main Content */}
      <div className="max-w-6xl mx-auto px-4 py-8">
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Left Sidebar */}
          <div className="space-y-6">
            {/* Welcome Card */}
            <div className="bg-zinc-900 border border-zinc-800 rounded-lg p-6">
              <h3 className="text-white text-lg font-semibold mb-2">
                Welcome, {userName}!
              </h3>
              <p className="text-zinc-400 mb-4">
                You're now part of the HIVE community. Start exploring and
                connecting!
              </p>
              <button className="w-full bg-yellow-500 hover:bg-yellow-600 text-black py-2 px-4 rounded-md font-medium transition-colors">
                ➕ Create your first post
              </button>
            </div>

            {/* Quick Actions */}
            <div className="bg-zinc-900 border border-zinc-800 rounded-lg p-6">
              <h3 className="text-white text-lg font-semibold mb-4">
                Quick Actions
              </h3>
              <div className="space-y-3">
                <button className="w-full flex items-center gap-3 text-zinc-400 hover:text-white p-2 rounded-md hover:bg-zinc-800 transition-colors">
                  👥 Join a space
                </button>
                <button className="w-full flex items-center gap-3 text-zinc-400 hover:text-white p-2 rounded-md hover:bg-zinc-800 transition-colors">
                  📅 Find events
                </button>
                <button className="w-full flex items-center gap-3 text-zinc-400 hover:text-white p-2 rounded-md hover:bg-zinc-800 transition-colors">
                  🔍 Discover people
                </button>
              </div>
            </div>
          </div>

          {/* Main Feed */}
          <div className="lg:col-span-2 space-y-6">
            {!hasContent ? (
              // Empty state
              <div className="bg-zinc-900 border border-zinc-800 rounded-lg p-8 text-center space-y-4">
                <div className="w-16 h-16 bg-yellow-500/20 rounded-full flex items-center justify-center mx-auto">
                  🏠
                </div>
                <h2 className="text-2xl font-bold text-white">
                  Your feed is ready!
                </h2>
                <p className="text-zinc-400 max-w-md mx-auto">
                  This is where you'll see posts from your spaces, campus
                  events, and updates from people you follow.
                </p>
                <div className="flex flex-col sm:flex-row gap-3 justify-center">
                  <button className="bg-yellow-500 hover:bg-yellow-600 text-black py-2 px-4 rounded-md font-medium transition-colors">
                    ➕ Share something
                  </button>
                  <button className="border border-zinc-700 text-white hover:bg-zinc-800 py-2 px-4 rounded-md transition-colors">
                    👥 Explore spaces
                  </button>
                </div>
              </div>
            ) : (
              // Content state
              <div className="space-y-6">
                {/* Post Composer */}
                <div className="bg-zinc-900 border border-zinc-800 rounded-lg p-6">
                  <div className="flex items-center gap-3 mb-4">
                    <div className="w-10 h-10 bg-yellow-500/20 rounded-full flex items-center justify-center">
                      {userName.charAt(0)}
                    </div>
                    <input
                      placeholder="What's happening at Stanford?"
                      className="flex-1 bg-zinc-800 border border-zinc-700 rounded-md px-4 py-2 text-white placeholder:text-zinc-500"
                      readOnly
                    />
                  </div>
                  <div className="flex items-center justify-between">
                    <div className="flex items-center gap-4 text-zinc-400">
                      <button className="hover:text-white">📷</button>
                      <button className="hover:text-white">📊</button>
                      <button className="hover:text-white">😊</button>
                      <button className="hover:text-white">📍</button>
                    </div>
                    <button className="bg-yellow-500 hover:bg-yellow-600 text-black px-4 py-1.5 rounded-md font-medium transition-colors">
                      Post
                    </button>
                  </div>
                </div>

                {/* Sample Posts */}
                <div className="bg-zinc-900 border border-zinc-800 rounded-lg p-6">
                  <div className="flex items-center gap-3 mb-4">
                    <div className="w-10 h-10 bg-zinc-700 rounded-full"></div>
                    <div>
                      <div className="text-white font-medium">
                        Campus Announcements
                      </div>
                      <div className="text-zinc-400 text-sm">2 hours ago</div>
                    </div>
                  </div>
                  <p className="text-zinc-300 mb-4">
                    Welcome to the new academic year! Join study groups, attend
                    events, and make the most of your campus experience. 🎓
                  </p>
                  <div className="flex items-center gap-6 text-zinc-400 text-sm">
                    <button className="flex items-center gap-1 hover:text-red-400">
                      ❤️ 42
                    </button>
                    <button className="flex items-center gap-1 hover:text-blue-400">
                      💬 8
                    </button>
                    <button className="flex items-center gap-1 hover:text-green-400">
                      🔄 3
                    </button>
                    <button className="flex items-center gap-1 hover:text-yellow-400">
                      🔖
                    </button>
                  </div>
                </div>

                <div className="bg-zinc-900 border border-zinc-800 rounded-lg p-6">
                  <div className="flex items-center gap-3 mb-4">
                    <div className="w-10 h-10 bg-blue-500/20 rounded-full flex items-center justify-center text-blue-400">
                      CS
                    </div>
                    <div>
                      <div className="text-white font-medium">
                        CS Study Group
                      </div>
                      <div className="text-zinc-400 text-sm">
                        4 hours ago • Computer Science
                      </div>
                    </div>
                  </div>
                  <p className="text-zinc-300 mb-4">
                    Looking for CS study partners for the upcoming midterms?
                    Join our Computer Science study group! We meet every Tuesday
                    and Thursday at 7 PM in the library. 📚
                  </p>
                  <div className="bg-zinc-800/50 border border-zinc-700 rounded-lg p-4 mb-4">
                    <div className="text-white font-medium mb-1">
                      CS Midterm Study Session
                    </div>
                    <div className="text-zinc-400 text-sm">
                      📅 Tomorrow, 7:00 PM • 📍 Main Library, Room 204
                    </div>
                  </div>
                  <div className="flex items-center gap-6 text-zinc-400 text-sm">
                    <button className="flex items-center gap-1 hover:text-red-400">
                      ❤️ 15
                    </button>
                    <button className="flex items-center gap-1 hover:text-blue-400">
                      💬 12
                    </button>
                    <button className="flex items-center gap-1 hover:text-green-400">
                      🔄 5
                    </button>
                    <button className="flex items-center gap-1 hover:text-yellow-400">
                      🔖
                    </button>
                  </div>
                </div>

                <div className="bg-zinc-900 border border-zinc-800 rounded-lg p-6">
                  <div className="flex items-center gap-3 mb-4">
                    <div className="w-10 h-10 bg-green-500/20 rounded-full flex items-center justify-center text-green-400">
                      M
                    </div>
                    <div>
                      <div className="text-white font-medium">Mike Chen</div>
                      <div className="text-zinc-400 text-sm">6 hours ago</div>
                    </div>
                  </div>
                  <p className="text-zinc-300 mb-4">
                    Just finished my first week at Stanford! The campus is
                    incredible and everyone has been so welcoming. Looking
                    forward to joining some clubs and making new friends! 🌟
                  </p>
                  <div className="grid grid-cols-2 gap-2 mb-4">
                    <div className="bg-zinc-800 rounded-lg h-32 flex items-center justify-center text-zinc-500">
                      📸 Photo
                    </div>
                    <div className="bg-zinc-800 rounded-lg h-32 flex items-center justify-center text-zinc-500">
                      📸 Photo
                    </div>
                  </div>
                  <div className="flex items-center gap-6 text-zinc-400 text-sm">
                    <button className="flex items-center gap-1 hover:text-red-400">
                      ❤️ 28
                    </button>
                    <button className="flex items-center gap-1 hover:text-blue-400">
                      💬 6
                    </button>
                    <button className="flex items-center gap-1 hover:text-green-400">
                      🔄 2
                    </button>
                    <button className="flex items-center gap-1 hover:text-yellow-400">
                      🔖
                    </button>
                  </div>
                </div>
              </div>
            )}
          </div>
        </div>
      </div>
    </div>
  );
};

const meta: Meta<typeof FeedPageComponent> = {
  title: "Pages/Feed",
  component: FeedPageComponent,
  parameters: {
    layout: "fullscreen",
    backgrounds: {
      default: "dark",
      values: [{ name: "dark", value: "#000000" }],
    },
  },
  argTypes: {
    userName: { control: "text" },
    userEmail: { control: "text" },
    hasContent: { control: "boolean" },
    loadingState: {
      control: { type: "select" },
      options: ["loading", "loaded"],
    },
  },
  decorators: [
    (Story) => (
      <div className="min-h-screen bg-background">
        <Story />
      </div>
    ),
  ],
};

export default meta;
type Story = StoryObj<typeof meta>;

export const EmptyState: Story = {
  args: {
    userName: "Sarah",
    userEmail: "sarah.johnson@stanford.edu",
    hasContent: false,
    loadingState: "loaded",
  },
};

export const WithContent: Story = {
  args: {
    userName: "Sarah",
    userEmail: "sarah.johnson@stanford.edu",
    hasContent: true,
    loadingState: "loaded",
  },
};

export const Loading: Story = {
  args: {
    userName: "Sarah",
    userEmail: "sarah.johnson@stanford.edu",
    hasContent: false,
    loadingState: "loading",
  },
};

export const NewUser: Story = {
  args: {
    userName: "Alex",
    userEmail: "alex.martinez@nyu.edu",
    hasContent: false,
    loadingState: "loaded",
  },
};
 